specVersion: "v1beta"
serviceId: "ev"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "ev-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
